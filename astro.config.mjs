// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://Jupresson.github.io/',
  // If deploying to https://Jupresson.github.io/ (user site), use base: '/'
  // If deploying to https://Jupresson.github.io/repo-name/ (project site), use base: '/repo-name'
  base: '/',
});